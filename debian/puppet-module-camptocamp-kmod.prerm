#!/bin/sh

set -e

if [ "${1}" = "remove" ] || [ "${1}" = "upgrade" ] || [ "${1}" = "deconfigure" ] ; then
	update-alternatives --remove puppet-module-kmod /usr/share/puppet/modules.available/camptocamp-kmod
fi

#DEBHELPER#

exit 0
